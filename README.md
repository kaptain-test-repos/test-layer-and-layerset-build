# Test Layer Build

E2E test repo for `layer-and-layerset-build` workflow (layer type) in buildon-github-actions.

Tests layer type with payload files, layer-payload validation, and schema validation. Layer source is in src/layer/ with a KaptainPM.yaml defining quality rules and a payload file.
